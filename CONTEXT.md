# 小光账号领域上下文

小光账号是本地优先的账号运营桌面软件。它统一管理账号资产、隔离登录态、公开作品数据、采集运行和人工发布入口；SQLite 是本地运行时的唯一数据主库。

## 领域词汇

- **账号（Account）**：已确认归属、进入正式账号列表和统计口径的平台身份。
- **登录会话（Login Session）**：添加账号期间的临时状态；成功前不得进入账号列表、作品数据或统计。
- **浏览器分区（Browser Partition）**：Electron 中隔离并持久化单个账号登录态的存储分区。
- **稳定身份（Stable Identity）**：平台账号 ID 或归一化公开主页 URL。昵称、头像和页面标题不能单独用于账号合并。
- **资料证据（Profile Evidence）**：从已登录创作者页面、公开主页或平台响应中提取的昵称、头像和稳定身份候选。
- **账号提升（Account Promotion）**：登录会话在资料证据完整后创建正式账号，或合并到已有稳定身份账号的原子过程。
- **平台能力（Platform Capability）**：某个平台的入口 URL、公开主页规则、稳定身份规则、视觉标识和采集支持声明。
- **采集目标（Collection Target）**：用户明确选择、允许进入采集运行的正式账号。
- **采集运行（Collection Run）**：从目标解析、身份预检、平台 adapter 执行到作品/快照/运行记录持久化的完整过程。
- **平台 adapter（Platform Adapter）**：在平台能力 seam 上实现特定平台响应匹配、作品解析和作者资料解析的实现。
- **人工发布工作区（Manual Publishing Workspace）**：复用账号浏览器分区打开发布页、作品页或主页；所有上传、填写和发布动作由用户完成。
- **设置快照（Settings Snapshot）**：经过验证并已持久化的完整运行设置，供前端、托盘和调度器共同使用。
- **运行时监督（Runtime Supervisor）**：负责本地 FastAPI 进程发现、身份握手、启动、日志、就绪与关闭的桌面模块。

## 不变量

1. 登录会话成功前不计入正式账号和分析口径。
2. 账号合并只依据稳定身份，不依据昵称。
3. 一个正式账号只有一个浏览器分区；提升时临时分区随账号采用。
4. 采集只读取公开页面，不绕过验证码、风控或访问限制。
5. 发布保持人工完成，不自动上传、填写或点击发布。
6. 正式数据目录与开发数据目录隔离；升级程序文件不得覆盖 `data/`。
7. 设置保存成功后，持久化状态与当前 Electron 运行状态必须一致。

## 关键 seam

- 账号生命周期 seam：登录会话、资料证据、账号提升、合并与清理。
- 平台能力 seam：共享声明加各运行时 adapter；多个平台使该 seam 真实存在。
- 采集运行 seam：手动、托盘、应用内定时和系统任务共用同一运行语义。
- 运行时 seam：开发 Python adapter 与打包后端 adapter 共用同一就绪接口。


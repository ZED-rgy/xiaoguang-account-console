# 安全政策

## 报告安全问题

请不要在公开 Issue 中提交包含真实账号、Cookie、浏览器 profile、访问令牌、数据库、日志或平台响应原文的安全问题。

仓库启用 GitHub 私密漏洞报告后，请优先通过仓库的 **Security → Report a vulnerability** 提交。若该入口暂不可用，请只提交不含敏感信息的最小公开描述，等待维护者提供私密沟通方式。

## 支持范围

当前仅维护最新的 `main` 分支。项目仍处于开发者预览阶段，不承诺对所有平台页面变化持续兼容。

## 数据安全边界

- FastAPI 默认只监听 `127.0.0.1`。
- 数据库、浏览器登录态、日志和备份默认保存在本地数据目录。
- 项目不要求上传 Cookie 或令牌到第三方服务。
- 使用者仍需保护本机账户、数据目录和操作系统权限。
